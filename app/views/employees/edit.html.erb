<h1>Edit Employee</h1>

<%= form_for @employee do |t| %> 
<%= t.label :name %> 
<%= t.text_area :name %>
<br>
<%= t.label :title %> 
<%= t.text_area :title %>
<br>
<%= t.label :company_id %> 
<%= t.collection_select :company_id, Company.all, :id, :name %>
<br>
<%= t.submit%>
<%end%> 